name: Discussion Posts via Buffer

on:
  schedule:
    # 火曜 09:10 JST（= 00:10 UTC）
    - cron: '10 0 * * 2'
    # 木曜 09:40 JST（= 00:40 UTC）
    - cron: '40 0 * * 4'
  workflow_dispatch:

concurrency:
  group: discussion-post
  cancel-in-progress: false

jobs:
  post:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install deps
        run: pip install -r requirements.txt

      - name: Post discussion through Buffer
        env:
          BUFFER_API_KEY: ${{ secrets.BUFFER_API_KEY }}
          BUFFER_ORGANIZATION_ID: ${{ secrets.BUFFER_ORGANIZATION_ID }}
          BUFFER_CHANNEL_ID: ${{ secrets.BUFFER_CHANNEL_ID }}
        run: python -m scripts.discussion_buffer

      - name: Save discussion rotation state
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add state_discussion.json
          if git diff --cached --quiet; then
            echo "No discussion state change"
            exit 0
          fi
          git commit -m "rotate discussion post [skip ci]"
          git pull --rebase
          git push
