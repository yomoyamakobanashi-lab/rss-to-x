name: Get X OAuth2 User Token (no terminal)

on:
  workflow_dispatch:
    inputs:
      redirect_full_url:
        description: "STEP2 用：X で許可後にブラウザのアドレス欄に出た URL を丸ごと貼り付け（未入力なら STEP1 を表示）"
        required: false
        default: ""

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: STEP1: 認可URLを作る（まずはこれをブラウザで開いて許可）
        if: ${{ inputs.redirect_full_url == '' }}
        env:
          CLIENT_ID: ${{ secrets.X_CLIENT_ID }}
        run: |
          if [ -z "$CLIENT_ID" ]; then
            echo "❌ X_CLIENT_ID が未設定です。Settings → Secrets → Actions で追加してください。"
            exit 1
          fi
          REDIRECT_URI="https://127.0.0.1/callback"
          SCOPES="tweet.read tweet.write users.read offline.access"
          # PKCE 生成
          CODE_VERIFIER=$(python3 - <<'PY'
import os,base64;print(base64.urlsafe_b64encode(os.urandom(40)).rstrip(b"=").decode())
PY
)
          CODE_CHALLENGE=$(python3 - <<PY
import hashlib,base64,os
cv=os.environ["CODE_VERIFIER"]
print(base64.urlsafe_b64encode(hashlib.sha256(cv.encode()).digest()).rstrip(b"=").decode())
PY
)
          echo "code_verifier=$CODE_VERIFIER" >> $GITHUB_ENV
          AUTH_URL="https://twitter.com/i/oauth2/authorize?response_type=code&client_id=${CLIENT_ID}&redirect_uri=${REDIRECT_URI}&scope=$(python3 - <<'PY'
print(__import__("urllib.parse").parse.quote("tweet.read tweet.write users.read offline.access"))
PY
)&state=xyz&code_challenge=${CODE_CHALLENGE}&code_challenge_method=S256"
          echo ""
          echo "🔗 このURLをコピーしてブラウザで開き、@_macobana で許可してください："
          echo "$AUTH_URL"
          echo ""
          echo "✅ 許可後に表示された https://127.0.0.1/callback?code=XXXX... の『URL全体』をコピーしてください。"
          echo "➡️ 次に、このワークフローをもう一度実行して、入力欄 redirect_full_url に貼り付けます。"
          exit 0

      - name: STEP2: code を取り出して token に交換
        if: ${{ inputs.redirect_full_url != '' }}
        env:
          CLIENT_ID: ${{ secrets.X_CLIENT_ID }}
          CLIENT_SECRET: ${{ secrets.X_CLIENT_SECRET }}
          REDIRECT_FULL: ${{ inputs.redirect_full_url }}
        run: |
          if [ -z "$CLIENT_ID" ] || [ -z "$CLIENT_SECRET" ]; then
            echo "❌ X_CLIENT_ID / X_CLIENT_SECRET が未設定です。Secrets を確認してください。"
            exit 1
          fi
          # URL から code を取り出す
          CODE=$(python3 - <<'PY'
import os,urllib.parse
u=os.environ["REDIRECT_FULL"]
qs=urllib.parse.urlparse(u).query
print(urllib.parse.parse_qs(qs).get("code",[None])[0] or "")
PY
)
          if [ -z "$CODE" ]; then
            echo "❌ code が読み取れませんでした。入力URLを見直してください。"
            exit 1
          fi
          REDIRECT_URI="https://127.0.0.1/callback"
          # token 交換
          RESP=$(curl -sS -X POST "https://api.x.com/2/oauth2/token" \
            -u "${CLIENT_ID}:${CLIENT_SECRET}" \
            -d "grant_type=authorization_code" \
            -d "client_id=${CLIENT_ID}" \
            -d "redirect_uri=${REDIRECT_URI}" \
            -d "code_verifier=dummy-not-needed-here" \
            -d "code=${CODE}")
          echo "[レスポンス] $RESP"
          ACCESS=$(python3 - <<'PY'
import json,os; j=json.loads(os.environ["RESP"]); print(j.get("access_token",""))
PY
)
          if [ -z "$ACCESS" ]; then
            echo "❌ access_token が取得できませんでした。User authentication settings と Callback URL を再確認してください。"
            exit 1
          fi
          echo ""
          echo "=================  取得した access_token  ================="
          echo "$ACCESS"
          echo "==========================================================="
          echo ""
          echo "👉 GitHub → Settings → Secrets → Actions → New repository secret"
          echo "   Name: X_BEARER_TOKEN"
          echo "   Value: （上の access_token を丸ごと貼り付け）"
