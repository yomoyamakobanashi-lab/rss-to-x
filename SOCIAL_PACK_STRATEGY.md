# ReelPal Social Pack v1

## 目的

1本のPodcastを「公開告知1回」で消費せず、LISTENで確認できる実際の会話から複数のX向けコンテンツへ分解する。

このレイヤーは通常の曜日投稿、毎日14:20の脱線クリップ、新着Podcast/note、映画ニュース連動とは別枠。

## データの流れ

1. ChatGPTの定期処理がLISTEN公開ページを確認する。
2. 最新回の本文・概要・文字起こしが十分に取得できた時だけ内容を読む。
3. タイトルだけから推測せず、実際に確認できた内容だけでSNSパックを生成する。
4. `data/social_pack_queue.json` に投稿候補を追加する。
5. GitHub Actionsが毎時07分にキューを確認し、期限到来済みの候補を1日最大1本だけBufferへ送る。
6. 1投目はリンクなし。LISTENリンクは返信へ置く。

## SNSパックの種類

### `three_hooks`

1回の中にある、互いに違う3つの引っかかりを短く並べる。

例の構造：

```text
今回のリルパル、ざっくり3つ。

① …
② …
③ なぜか…
```

### `host_split`

マコとオーマが本当に異なる結論・評価・解釈へ着地した時だけ作る。

- disagreementを捏造しない。
- 単なる言い回しの違いを「対立」にしない。
- 誰がどちらの立場だったかLISTENで確認できない場合は作らない。

### `episode_hook`

その回を未聴の人でも答えられる、一番強い問い・違和感を1つだけ独立させる。

### `winner_remix`

実績の良かった過去投稿を30日以上空け、別角度から再編集する。

- 元文の単純な言い換えは禁止。
- 時事依存・訃報・事故・不祥事等は再利用しない。
- 同じ投稿をそのまま再掲しない。

## 投稿上限

`state_social_pack.json` により、このレイヤーからのルート投稿はJSTで1日最大1本。

キューが複数たまっていても翌日以降へ持ち越す。

## 勝ち投稿判定

日次analytics後に `scripts/winner_candidates.py` を実行する。

- 投稿後72時間以上
- 60日以内
- 80 imp以上
- 同形式の中央値（同形式3件未満なら全対象中央値）の1.5倍以上
- 外部リンクを含まない投稿

該当候補を `data/winner_candidates.json` に出力する。

候補を自動でそのまま再投稿はしない。意味を読んで別角度へ再編集してから `winner_remix` としてキューへ入れる。

## ネイティブ音声クリップ

金曜10:20 JSTに週1回だけ実験する。

1. `data/audio_clip_queue.json` の未使用候補を選ぶ。
2. LISTENのエピソードタイトルをRSSと照合する。
3. RSS enclosureの元音源から15〜35秒をffmpegで切り出す。
4. 1200×676の番組カード＋音声波形をH.264/AACのMP4へ変換する。
5. `media/audiograms/` へ公開し、Bufferへネイティブ動画として送る。
6. 返信に対応するLISTEN URLを置く。
7. Buffer受理後のみ `state_audio_clip.json` を更新する。

LISTENとRSSのタイトル照合スコアが0.48未満なら安全側で停止する。

## 分析カテゴリ

日次分析では以下を独立集計する。

- `social_pack_three_hooks`
- `social_pack_host_split`
- `social_pack_episode_hook`
- `winner_remix`
- `audiogram`

これらを既存の `discussion`、`quick_reply`、`funny_clip` 等と中央値imp・ER・返信・RPで比較する。

## 原則

- 内容照会の一次資料はLISTEN。
- タイトルだけで発言を生成しない。
- 直接引用より要約・再構成を基本とする。
- 面白くするために発言の意味を反転させない。
- 同じ素材を短期間に何度も使わない。
- Xの投稿数を増やすこと自体を目的にしない。各形式の実測値を見て残す／減らすを判断する。
